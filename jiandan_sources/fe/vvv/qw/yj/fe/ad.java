package fe.vvv.qw.yj.fe;

import androidx.annotation.NonNull;
import androidx.annotation.RequiresApi;
import java.util.Arrays;

@RequiresApi(21)
public class ad {
    @NonNull
    public static de ad(long j, @NonNull de deVar) {
        return new yj(j, deVar);
    }

    @NonNull
    public static de de(@NonNull de... deVarArr) {
        return new uk(Arrays.asList(deVarArr));
    }

    @NonNull
    public static de qw(@NonNull de... deVarArr) {
        return new th(Arrays.asList(deVarArr));
    }
}
