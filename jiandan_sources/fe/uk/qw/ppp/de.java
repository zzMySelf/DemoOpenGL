package fe.uk.qw.ppp;

import androidx.annotation.CheckResult;
import androidx.annotation.NonNull;
import com.dxmbumptech.glide.load.Key;
import fe.uk.qw.pf.fe.yj;

public class de extends qw<de> {
    @CheckResult
    @NonNull
    public static de H(@NonNull Class<?> cls) {
        return (de) new de().rg(cls);
    }

    @CheckResult
    @NonNull
    public static de I(@NonNull yj yjVar) {
        return (de) new de().th(yjVar);
    }

    @CheckResult
    @NonNull
    public static de J(@NonNull Key key) {
        return (de) new de().z(key);
    }
}
